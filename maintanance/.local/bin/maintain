#!/bin/bash
sudo echo nothing > /dev/null

yay -Syu --noconfirm
sudo pacman --noconfirm -Rns $(pacman -Qtdq)
docker system prune -f

rm ~/.cache/yay -fr
mkdir ~/.cache/yay

rm ~/.cache/vmware/drag_and_drop
mkdir ~/.cache/vmware/drag_and_drop
